<div class="mb-3">
    <label for="{{fieldName}}" class="form-label">{{fieldLabel}}</label>
    <select name="{{fieldName}}" id="{{fieldName}}" class="form-select @error('{{fieldName}}') is-invalid @enderror">
        {{options}}
    </select>
    @error('{{fieldName}}')
        <div class="invalid-feedback">{{ $message }}</div>
    @enderror
</div>

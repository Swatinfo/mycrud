<div class="mb-3">
    <label for="{{fieldName}}" class="form-label">{{fieldLabel}}</label>
    <input type="email" name="{{fieldName}}" id="{{fieldName}}" class="form-control @error('{{fieldName}}') is-invalid @enderror" value="{{fieldValue}}">
    @error('{{fieldName}}')
        <div class="invalid-feedback">{{ $message }}</div>
    @enderror
</div>
